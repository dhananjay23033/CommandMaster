#ifndef LS_H
#define LS_H

#include <bits/stdc++.h>

using namespace std;

class LS {
public:
  void execute(istream&, filesystem::path), executeR(bool, filesystem::path), print(bool, filesystem::path);
};

#endif
